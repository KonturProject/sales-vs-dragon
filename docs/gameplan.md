# Отделы против дракона — описание проекта (текущее состояние)

Карта файлов: [MAP.md](./MAP.md). Технические решения и их обоснование: [tech.md](./tech.md). Концепция, правила механики и история переезда со «свина в загон»: [dragon-track.md](./dragon-track.md). (Репозиторий и URL Pages называются `sales-vs-dragon` — переименованы 2026-09-20 из `svin-i-zagon`, названия исходной игры.)

## Концепция

Мини-игра-визуализация для отдела продаж на офисном экране, в пиксель-арт стиле. Герои-маскоты 6 отделов отвоёвывают золото у дракона, сидящего на горе золота (= план). Когда отдел приносит деньги (новая продажа в Google-таблице) — его герой бьёт дракона. Неделя разбита на 5 равных дневных планов; дракон пятиголовый, и каждый пересечённый дневной план (20/40/60/80/100 % недельного) отрубает ему голову. Когда план недели закрыт — голов нет, на экране «Отделы победили дракона! План недели выполнен». Руководитель филиала (Круэлла) стоит в стороне, не привязана к отделу — подбадривает каждую продажу и празднует победу. Админка с PIN задаёт недельный план.

### Отделы → герои

| Код в таблице | Герой |
|---|---|
| СР1 | Лев «Актион» |
| СР2 | Скрудж (королева) |
| СР3 | Гринч |
| СР5 | Йода (девочка-джедай) |
| СР6 | Незнайка с деньгами (бизнес-леди) |
| СР9 | Миньон |

## Источник правды по данным

Приватная Google-таблица → Google Apps Script Web App (standalone, не через Extensions-меню) → агрегированный JSON (сумма за неделю, разбивка по РОПам, план) → фронтенд опрашивает раз в ~15 сек. Реальные ссылки и PIN — см. [MAP.md](./MAP.md#apps-script--бэкенд).

## Статус

### Готово и проверено вживую (трек «Дракон», 2026-09-20)
- [x] Новый ростер и раскладка: 6 героев слева в два шахматных ряда (не налезают друг на друга), дракон на горе золота справа (статичный), Круэлла в стороне; все герои бьют вправо, дракон смотрит влево
- [x] Фон-пещера (`фон.jfif`), холст 1280×720
- [x] Пайплайн ассетов `game/tools/build-sprites.py`: обрезка, единый масштаб поз удара, даунскейл, совмещение 6 состояний дракона
- [x] Механика голов: `headsRemaining` из `ratio × 5`, baseline при загрузке, потеря голов с каскадом при нескольких порогах, самовосстановление при новой неделе
- [x] Удары героев по очереди с позами (round-robin `hit1..N`): герой **добегает до подножия горы под драконом** (не 30 % пути), удар — по прибытии, затем разворот и возврат; реакция дракона (вспышка, сквош, тряска, брызги монет) — в момент удара
- [x] Потеря головы (красная вспышка, тряска камеры, искры, звук), победа (серый дракон, конфетти, баннер, празднуют все герои и Круэлла)
- [x] HUD: полоса недели с делениями «день 1…5», счётчик голов, живой рейтинг отделов, индикатор связи, кнопка звука; **чёткие шрифты** (буфер 2560×1440, текст `resolution: 2`, крупнее кегль — см. tech.md «Чёткость текста»)
- [x] Админка: PIN-гейт, форма плана, «Сейчас: X / Y ₽»
- [x] Apps Script backend (публичный URL в `config.json`), задеплоенный на GitHub Pages сайт (**с 2026-09-20 показывает версию с драконом**)
- [x] Процедурный звук (Web Audio), по умолчанию выключен; кнопка mute с персистентностью
- [x] Idle-жизнь героев и Круэллы: они стоят на полу (покачивание вверх-вниз убрано), раз в 20–30 с — лёгкий наклон; мерцающий фон

### Известные гэпы / отложено
- [ ] Не проверен напрямую точный формат кода отдела в реальной таблице (регистр/пробелы у «СР1» и т.д.) — если чей-то герой не бьёт при реальной продаже, смотреть сюда первым
- [ ] Круэлла имеет один арт — её реакции только код-анимации (пульс/искры)
- [ ] Скрудж — одна поза удара (остальным по 3)
- [ ] Дневные планы делятся поровну от недельного; возможность неравных долей/реальных «дней» пока не нужна

### Состояние работы (2026-09-20, после деплоя)
- **Версия с драконом закоммичена (`aba4f65`, ветка `master`) и задеплоена** на GitHub Pages (`npm run deploy`, ветка `gh-pages`); на живом сайте проверено: заголовок, буфер 2560×1440, реальные данные (4 головы из 5), статус «в эфире».
- **Следующие шаги**: (1) **сменить PIN админки** — он был записан открытым текстом в `docs/MAP.md` и лежит в публичной истории git (первый коммит `4b22d28` и `27f16d2`); из рабочей копии убран, но из истории уже не вычистить (форки/кэш) — надёжное решение только новый PIN через `setAdminPin_()` в редакторе Apps Script; (2) при желании — новые позы удара Скруджу и реакции Круэлле; (3) при следующих правках визуала — цикл «правка → проверка в превью → документация → коммит/деплой по явному «да»».

### Админка (2026-09-21)
- [x] Вход по PIN (проверка на бэкенде), недельный план
- [x] Чтение таблицы: последние 50 строк `Sales` + итоги недели/по отделам
- [x] Внесение оплаты из админки (отдел, сумма, дата; подтверждение; защита от дублей и формул)
- [x] Кнопки анимаций для экранов (удар отдела, упасть/заснуть, поднять всех, рычание, конфетти, «все празднуют») — таблицу не затрагивают, доходят за ≤15 с
- [x] Смена PIN из админки (хеш, блокировка после 5 ошибок). Сам PIN пока не менялся — **по-прежнему нужно сменить** (старый лежит в публичной истории git)
- Бэкенд (версия 3) **уже развёрнут** в Apps Script (2026-09-21), совместим со старой админкой на сайте. Проведена сессия отладки с настоящим PIN (8 найденных проблем исправлены, список — `docs/tech.md`, «Отладка»). Новая админка и игровая часть команд попадут на сайт после `npm run deploy`. Детали, тесты и что не проверялось на живой таблице — `docs/tech.md`, `apps-script/README.md`.

### Анимации простоя и рычание (2026-09-21)
- [x] Нет продаж час и дольше → случайные герои падают на пол или засыпают («Zzz»), по одному за час, до 4; любая продажа будит всех
- [x] Дракон рычит и угрожающе дрожит (звук + дрожь + гул камеры), чаще при долгом простое
- Считается по всей команде (не по отделам), в игровом времени с нуля при загрузке страницы. Пробный показ: `?idle=1` в адресе. Детали — `docs/tech.md`.

### Производительность (2026-09-21)
- Сотрудники жаловались: на слабых ноутбуках (i3, встроенная графика) игра грузит систему на 60–100 %. Причина — постоянная перерисовка неподвижной сцены без ограничения частоты в буфере 2560×1440. Исправлено: профиль качества по устройству, частота кадров по активности (в покое ~10 к/с), никаких лишних анимаций на пустых опросах. Подробности и ограничения — `docs/tech.md`, «Производительность». **Нужно проверить у сотрудников после деплоя** (Диспетчер задач в покое).

## Проверка на реальных данных

Вся связка (Sheet → Apps Script → игра → анимации) проверена на живых данных: на 2026-09-20 план 1 054 000 ₽, набрано 232 000 ₽ (СР1) — дракон при загрузке страницы корректно показывается с 4 головами.
